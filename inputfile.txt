vector.<int> vint = vector.<int>(20);

vint[0] = 1;

debug vint;

vector.<string> vstring = vector.<string>(10);

int i;
int j = 10;

for(i=0; i < j ; i++)
{
	vstring[i] = "test ";
}


debug vstring;

debug "finished";